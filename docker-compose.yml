services:
  app:
    image: ghcr.io/yeluonight/cf-manager:latest
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      ENCRYPTION_KEY: changeme
      API_SECRET: changeme
      PROXY_URL: ""
      DB_PATH: /app/data/cf-manager.db
      CORS_ORIGINS: ""
    volumes:
      - cf-data:/app/data

volumes:
  cf-data:
    driver: local
